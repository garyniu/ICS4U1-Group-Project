import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Similar to BoomingBusinessImage, the corresponding event subclass already has its image set (the arrow animation). My simple solution for adding an event 
 * icon into the world is by adding an actor with their image set as the event icon. 
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class StockMarketCrashImage extends Effect
{
    public StockMarketCrashImage(){
        getImage().scale(100,100); 
    }
    /**
     * Act - do whatever the StockMarketCrashImage wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        // Add your action code here.
    }
}
